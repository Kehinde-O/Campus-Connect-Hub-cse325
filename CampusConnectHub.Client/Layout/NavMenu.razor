@using CampusConnectHub.Client.Services
@using CampusConnectHub.Shared.DTOs
@inject AuthService AuthService
@inject NavigationManager Navigation

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/" aria-label="Campus Connect Hub - Home">
            <span class="brand-gradient" style="font-size: 1.5rem; font-weight: bold;">C</span>ampus Connect Hub
        </a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu" aria-label="Toggle navigation menu" aria-expanded="@(!collapseNavMenu)" type="button">
            <span class="navbar-toggler-icon" aria-hidden="true"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    @if (isAuthenticated && user != null)
    {
        <div class="user-profile-section">
            <div class="user-profile-card">
                <div class="user-avatar">
                    @(user.FirstName?.Substring(0, 1).ToUpper() ?? "U")
                </div>
                <div class="user-name">@user.FirstName @user.LastName</div>
                <div class="user-role">@user.Role</div>
            </div>
        </div>
    }
    
    <nav class="flex-column" aria-label="Main navigation">
        <div class="nav-item">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All" aria-label="Go to news feed">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> News Feed
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="events" aria-label="Go to events">
                <span class="bi bi-calendar-event" aria-hidden="true"></span> Events
            </NavLink>
        </div>
        <div class="nav-item">
            <NavLink class="nav-link" href="resources" aria-label="Go to resources">
                <span class="bi bi-link-45deg" aria-hidden="true"></span> Resources
            </NavLink>
        </div>
        @if (isAuthenticated)
        {
            <div class="nav-item">
                <NavLink class="nav-link" href="events/calendar" aria-label="Go to calendar">
                    <span class="bi bi-calendar3" aria-hidden="true"></span> Calendar
                </NavLink>
            </div>
            <div class="nav-item">
                <NavLink class="nav-link" href="my-rsvps" aria-label="Go to my RSVPs">
                    <span class="bi bi-calendar-check" aria-hidden="true"></span> My RSVPs
                </NavLink>
            </div>
            <div class="nav-item">
                <NavLink class="nav-link" href="profile" aria-label="Go to profile">
                    <span class="bi bi-person-circle" aria-hidden="true"></span> Profile
                </NavLink>
            </div>
            @if (isAdmin)
            {
                <div class="nav-item">
                    <NavLink class="nav-link" href="admin" aria-label="Go to admin dashboard">
                        <span class="bi bi-speedometer2" aria-hidden="true"></span> Admin
                    </NavLink>
                </div>
            }
            <div class="nav-item">
                <button class="nav-link btn-link" @onclick="HandleLogout" aria-label="Logout" type="button">
                    <span class="bi bi-box-arrow-right" aria-hidden="true"></span> Logout
                </button>
            </div>
        }
        else
        {
            <div class="nav-item">
                <NavLink class="nav-link" href="login" aria-label="Go to login">
                    <span class="bi bi-box-arrow-in-right" aria-hidden="true"></span> Login
                </NavLink>
            </div>
            <div class="nav-item">
                <NavLink class="nav-link" href="register" aria-label="Go to registration">
                    <span class="bi bi-person-plus" aria-hidden="true"></span> Register
                </NavLink>
            </div>
        }
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;
    private bool isAuthenticated = false;
    private bool isAdmin = false;
    private UserResponse? user;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    protected override async Task OnInitializedAsync()
    {
        await CheckAuth();
    }

    private async Task CheckAuth()
    {
        isAuthenticated = await AuthService.IsAuthenticatedAsync();
        if (isAuthenticated)
        {
            user = await AuthService.GetUserAsync();
            isAdmin = user?.Role == "Administrator";
        }
    }

    private async Task HandleLogout()
    {
        await AuthService.LogoutAsync();
        Navigation.NavigateTo("/");
    }
}
